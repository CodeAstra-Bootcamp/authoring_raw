<%= simple_form_for [@book, @chapter, @section] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.submit "#{@section.new_record? ? 'Add' : 'Update'} Section", class: "btn btn-primary btn-lg" %>
<% end %>
